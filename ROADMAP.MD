# 25-11-2025

- sudo apt-get install libboost-all-dev
- sudo apt install libssl-dev
- rm -rf build
- mkdir build
- cd build
- cmake ..
- make

- To run just write this on terminal in build folder ./server and then enter
